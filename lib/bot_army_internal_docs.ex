defmodule BotArmyInternalDocs do
  @moduledoc """
  Internal Docs Bot — indexes internal documentation into pgvector for
  semantic search and exposes a NATS query interface for Synapse context.
  """
end
